---
title: "DATA607 Project 1 -Elo "
author: "David Simbandumwe"
output:
  html_document:
    includes:
      in_header: ../../assets/header.html
    css: ../../assets/mtheme_max.css
    highlight: pygments
    theme: cerulean
    toc: true
    toc_float: true
  pdf_document: default
editor_options: 
  
  chunk_output_type: console
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(readtext)
```


#Summary
In this project, you’re given a text file with chess tournament results where the information has some structure. Your job is to create an R Markdown file that generates a .CSV file (that could for example be imported into a SQL database) with the following information for all of the players:
Player’s Name, Player’s State, Total Number of Points, Player’s Pre-Rating, and Average Pre Chess Rating of Opponents
For the first player, the information would be:
Gary Hua, ON, 6.0, 1794, 1605
1605 was calculated by using the pre-tournament opponents’ ratings of 1436, 1563, 1600, 1610, 1649, 1663, 1716, and dividing by the total number of games played.
If you have questions about the meaning of the data or the results, please post them on the discussion forum. Data science, like chess, is a game of back and forth...
The chess rating system (invented by a Minnesota statistician named Arpad Elo) has been used in many other contexts, including assessing relative strength of employment candidates by human resource departments.



```{r, }

eloTxt <- readtext("https://raw.githubusercontent.com/dsimband/DATA607/main/Project1/tournamentinfo.txt")

e2 <- scan("https://raw.githubusercontent.com/dsimband/DATA607/main/Project1/tournamentinfo.txt", 
           character(0), 
           quote = NULL,
           sep = "\n")
e2





e3 <- scan("https://raw.githubusercontent.com/dsimband/DATA607/main/Project1/tournamentinfo.txt", 
           character(0), 
           quote = NULL,
           sep = "|")
e3







df <- data.frame(matrix(ncol = 5, nrow = 0))
x <- c("name", "state", "total_points","pre_rating","post_rating")
colnames(df) <- x


df <- data.frame( name = character(),
                  state = character(),
                  total_points = double(),
                  pre_rating = double(),
                  post_rating = double()
)

for (i in 4:13) {
  if (!str_detect(e2[i],"\\-") ) {
    
    g <- e2[i] %>% str_split("\\|", simplify = TRUE) 
    g1 <- str_squish(e2[i+1]) %>% str_split(" ", simplify = TRUE) 
    
    df %>% add_row(
      name = str_trim(g[2]),
      state = str_trim(g1[1]),
      total_points = as.numeric(g[3]),
      pre_rating = as.numeric(g1[6]),
      post_rating = as.numeric(str_replace(g1[7],"->",""))
    )
  }
}






i = 26
while (i < length(e3)){
  print(e3[i])
  i = i+23
}


for (i in 1:length(e2)) {

  print(e2[i])
}




for (i in 1:13) {
  g <- e2[i] %>% str_split("\\|")
  print(g)
}

for (i in 4:length(e2)) {
  if (!str_detect(e2[i],"\\-") ) {
    g <- e2[i] %>% str_split("\\|", simplify = TRUE) 
    print(str_trim(g[2]))
    print(as.numeric(g[3]))
    
    g1 <- str_squish(e2[i+1]) %>% str_split(" ", simplify = TRUE) 
    print(g1[6])
    print(as.numeric(g1[6]))
    print(g1[7])
    print(as.numeric(str_replace(g1[7],"->","")))
    
    
  }
}



for (i in 4:length(e2)) {
  if (!str_detect(e2[i],"\\-") ) {
    g <- e2[i] %>% str_split("\\|", simplify = TRUE) 
    #print(str_trim(g[2]))
    #print(as.numeric(g[3]))
    
    g1 <- str_squish(e2[i+1]) %>% 
      #str_replace("->"," -> ") %>%
      str_split(" ", simplify = TRUE) 
    
    if (is.numeric(g1[6])) {
       print(as.numeric(g1[6]))
    }
    else {
      print(g1[6] %>% str_split("->", simplify = TRUE) )
    }
   
  }
}



n1 <- regex("
  \\R:     # R
  [:digit:]     # number
  ", comments = TRUE)


for (i in 4:length(e2)) {
  if (!str_detect(e2[i],"\\-") ) {
    
    print(str_extract_all(e2[i+1],n1))

    
    #rindex <- str_locate(e2[i+1],"R:")
    #print(rindex[2])
    
    #pindex <- str_locate(e2[i+1],"->")
    #print(pindex[2])
    
    #print(str_subset(e2[i+1], "R:") )
    
    g <- e2[i] %>% str_split("\\|", simplify = TRUE) 
    #print(str_trim(g[2]))
    #print(as.numeric(g[3]))
    
    g1 <- str_squish(e2[i+1]) %>% 
      #str_replace("->"," -> ") %>%
      str_split(" ", simplify = TRUE) 
    
    if (is.numeric(g1[6])) {
       #print(as.numeric(g1[6]))
    }
    else {
      #print(g1[6] %>% str_split("->", simplify = TRUE) )
    }
   
  }
}




?str_squish

s1 <- str_squish(" R: 13131243 -> 13142341")
s1



s1 <-    "  MI | 15619130 / R: 1220P13->1416P20  |N:3  |B    |B    |W    |W    |B    |B    |W    |"
s1 <- gsub(" ","",s1)
stateTmp <- str_extract(s1,"[:alpha:]." )
preTmp <- str_extract(s1,"R:\\d..." )
preTmp <- str_extract(preTmp,"\\d+" )
postTmp <- str_extract(postTmp,">\\d..." )
postTmp <- str_extract(postTmp,"\\d+" )













gsub(" ", "", "R: 13131243 ->    13142341    ")

?print

fields <- c("Name: Hadley", "Country: NZ", "Age: 35")
fields %>% str_split(": ", simplify = TRUE)



















df <- data.frame( name = character(),
                  state = character(),
                  total_points = double(),
                  pre_rating = double(),
                  post_rating = double()
)



e2 <- scan("https://raw.githubusercontent.com/dsimband/DATA607/main/Project1/tournamentinfo.txt", 
           character(0), 
           quote = NULL,
           sep = "\n")


for (i in 4:length(e2)) {
  if (!str_detect(e2[i],"\\-") ) {
    
    g <- e2[i] %>% str_split("\\|", simplify = TRUE) 
    #g1 <- str_squish(e2[i+1]) %>% str_split(" ", simplify = TRUE) 
    
    g1 <- gsub(" ","",e2[i+1])
    stateTmp <- str_extract(g1,"[:alpha:]." )
    preTmp <- str_extract(g1,"R:\\d..." )
    preTmp <- str_extract(preTmp,"\\d+" )
    postTmp <- str_extract(g1,">\\d..." )
    postTmp <- str_extract(postTmp,"\\d+" )    
    

    df <- df %>% add_row(
      name = str_trim(g[2]),
      state = str_trim(stateTmp),
      total_points = as.numeric(g[3]),
      pre_rating = as.numeric(preTmp),
      post_rating = as.numeric(postTmp)
    )
    
    

    
    
    
  }
}






```

